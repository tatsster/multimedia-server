{
	# Current setup exposes Caddy admin on all interfaces.
	# Only keep this if the admin endpoint is protected by firewall/LAN rules.
	admin 0.0.0.0:2019

	# Optional: use Caddy as DDNS updater with Cloudflare.
	# Requires Caddy built with:
	#   github.com/mholt/caddy-dynamicdns
	#   github.com/caddy-dns/cloudflare
	dynamic_dns {
		provider cloudflare {env.CF_API_TOKEN}
		domains {
			example.com service1 service2 service3
		}
		dynamic_domains
		versions ipv4
	}
}

*.example.com {
	# Wildcard certificate using Cloudflare DNS challenge.
	# Secret source: Cloudflare profile -> API Tokens -> custom token with Zone:DNS:Edit and Zone:Zone:Read.
	tls {
		dns cloudflare {env.CF_API_TOKEN}
		propagation_delay 2m
		resolvers 1.1.1.1
	}

	# Example HTTP backend, copied from the current pattern used for Jellyfin.
	@jellyfin host jellyfin.example.com
	handle @jellyfin {
		reverse_proxy http://LAN_SERVICE_IP:8096
	}

	# Example HTTPS backend with self-signed/internal certificate.
	# Use tls_insecure_skip_verify only for trusted LAN services.
	@vaultwarden host vaultwarden.example.com
	handle @vaultwarden {
		reverse_proxy https://LAN_SERVICE_IP:8000 {
			transport http {
				tls
				tls_insecure_skip_verify
			}
		}
	}

	# Example utility service.
	@flaresolverr host flaresolverr.example.com
	handle @flaresolverr {
		reverse_proxy http://LAN_SERVICE_IP:8191
	}
}
