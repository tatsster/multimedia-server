# Live LXC Audit Example

This is a sanitized example of the file generated by:

```bash
./scripts/pve/audit-lxcs.sh
```

The real generated file is ignored by Git:

```text
inventory/live-lxc-audit.md
```

Use the real audit only as local working material, then copy verified non-secret values into [`lxc-map.md`](./lxc-map.md).

## Summary table

| CT ID | Name | Status | IP config | Unprivileged | Features | Creation hint |
|---|---|---|---|---|---|---|
| 100 | proxy-caddy | running | `name=eth0; bridge=vmbr0; ip=<ip>/<cidr>; gw=<gateway>` | 0 | `nesting=1` | Community Scripts: Caddy |
| 101 | proxy-cloudflared | running | `name=eth0; bridge=vmbr0; ip=<ip>/<cidr>; gw=<gateway>` | 0 | `nesting=1` | Community Scripts: Cloudflared |
| 120 | hermes | running | `name=eth0; bridge=vmbr0; ip=<ip>/<cidr>; gw=<gateway>` | 0 | `nesting=1` | Manual repo script: create-hermes-lxc.sh |

## Detailed sanitized configs

### CT 120 — hermes

```text
arch: amd64
cores: <cores>
features: nesting=1
hostname: hermes
memory: <memory-mb>
net0: name=eth0,bridge=vmbr0,ip=<ip>/<cidr>,gw=<gateway>
onboot: 1
ostype: debian
rootfs: <storage>:subvol-<ctid>-disk-0,size=<size>
swap: <swap-mb>
unprivileged: 0
```

Do not paste real secrets, tokens, private notes, or unnecessary MAC addresses into committed docs.
