--- /usr/lib/python3/dist-packages/glances/plugins/sensors/__init__.py.orig
+++ /usr/lib/python3/dist-packages/glances/plugins/sensors/__init__.py
@@ -138,6 +138,13 @@
             stat["label"] = self.__get_alias(stat)
+            # Homepage local policy: show Package/Core CPU temp warning at 80C.
+            # Glances' API exposes kernel coretemp high (100C) as warning by default;
+            # Homepage uses that field for its expanded Warn value and temp bar.
+            if stat.get('type') == 'temperature_core' and stat.get('label', '').startswith(('Package id', 'Core')):
+                stat['warning'] = 80
+                stat['critical'] = 90
             # Add the stat to the stats_transformed list
             stats_transformed.append(stat)
