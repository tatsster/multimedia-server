services:
  glance:
    image: glanceapp/glance
    restart: unless-stopped
    volumes:
      - /docker/glance:/app/config
    env_file:
      - stack.env
    environment:
      - TZ=${TZ:-Asia/Ho_Chi_Minh}
    ports:
      - 8081:8080
